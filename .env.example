# Get a free API key at https://console.groq.com
# The app works without it (falls back to extractive summaries),
# but AI summaries make it much better.
GROQ_API_KEY=your_groq_api_key_here

# Server port (default: 3000)
PORT=3000

# Admin authentication (protects /admin dashboard and admin API)
# If not set, admin is open (dev mode)
ADMIN_TOKEN=your_secret_admin_token

# Public URL for the app (used in unsubscribe links in digest emails)
# No trailing slash
BASE_URL=https://your-app.up.railway.app

# Email digest configuration
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your.email@gmail.com
# SMTP_PASS=your-16-char-app-password
# DIGEST_RECIPIENTS=analyst@ngo.org,researcher@university.edu
# DIGEST_FROM_NAME=Horn Monitor
